{
  "name": "docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tinacms dev -c \"hugo server -D --config config.toml --themesDir themes --theme hugo-geekdoc -p 1313\"",
    "build": "tinacms build && hugo server -D --config config.toml --themesDir themes --theme hugo-geekdoc -p 1313"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tinacms/cli": "^1.5.21",
    "tinacms": "^1.5.12"
  },
  "devDependencies": {
    "@types/node": "^20.3.2"
  }
}
